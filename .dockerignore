node_modules
.next
.git
.github
.gitignore
.dockerignore
.swc
data/
monitoring/prometheus_data/
monitoring/grafana_data/
backups/
*.log
*.sql
*.gz
out/
dist/
build/
cov/
coverage/
.nyc_output
.playwright
playwright-report
test-results

# Env files must never enter image build contexts: real secrets stay on the
# host (compose env_file / k8s Secrets inject them at runtime), and builds
# stay hermetic — identical locally and in CI checkouts.
envs/
