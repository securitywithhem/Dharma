{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dharma.serviceAccountName" . }}
  labels:
    {{- include "dharma.labels" . | nindent 4 }}
{{- end }}
