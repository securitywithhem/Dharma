# ============================================================
# monitoring/prometheus.yml
# Prometheus scrape configuration for Dharma services.
# ============================================================

global:
  scrape_interval:     15s   # How often to scrape targets
  evaluation_interval: 15s   # How often to evaluate rules
  scrape_timeout:      10s
  external_labels:
    environment: 'development'
    service: 'dharma'

# ── Alerting (optional – configure Alertmanager separately) ──────────────
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets: ['alertmanager:9093']

# ── Rule files ────────────────────────────────────────────────────────────
rule_files:
  - "rules/*.yml"

# ── Scrape configurations ─────────────────────────────────────────────────
scrape_configs:

  # Prometheus self-metrics
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          component: 'prometheus'

  # PostgreSQL exporter (postgres-exporter)
  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres-exporter:9187']
        labels:
          component: 'database'
          db: 'dharma_db'
    scrape_interval: 30s

  # Redis exporter (redis-exporter)
  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']
        labels:
          component: 'cache'
    scrape_interval: 15s

  # Application metrics (Next.js app + worker) pushed via OTLP to the
  # OpenTelemetry Collector, which re-exposes them here. Enable by setting
  # OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318 on nextjs/worker.
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']
        labels:
          component: 'application'
    scrape_interval: 15s

  # MinIO metrics
  - job_name: 'minio'
    metrics_path: '/minio/v2/metrics/cluster'
    scheme: http
    static_configs:
      - targets: ['minio:9000']
        labels:
          component: 'storage'
    scrape_interval: 30s

  # Caddy metrics (via Caddy admin endpoint)
  - job_name: 'caddy'
    static_configs:
      - targets: ['caddy:2019']
        labels:
          component: 'proxy'
    scrape_interval: 30s
